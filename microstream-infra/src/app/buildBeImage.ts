import { exec } from "child_process"

import * as fs from "fs"
import { promisify } from "util"

// ensure that Dockerfile in be exists
// build it with pulumi

// const child = exec(`sbt "docker:stage"`)

// child.stdout.pipe(process.stdout)
